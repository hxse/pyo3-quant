# AGENTS.md - 协作守则与技术指南

本项目是一个基于 **PyO3** 开发的高性能量化交易回测框架。**Rust** 承载核心计算逻辑（如指标计算、回测核心），**Python** 负责上层策略逻辑与灵活调用。

## 1. 沟通与语言规范
*   **全程中文沟通**：所有分析、交流、回复、沟通、Plan 和报告均必须使用中文。
*   **代码与注释**：代码本身（变量名、函数名、类名等）应使用**英文**以保持专业性；代码注释必须使用**中文**以确保易读性和审阅效率。
*   **强制注释**：写代码时必须带上注释，提高可阅读性，让审阅与后续维护更易懂。

## 2. 方案讨论与 Plan 优先
*   **开发范式定义（核心）**：
    *   本项目采用**方案驱动开发**，不是“任务驱动开发”，也不是“文档驱动开发”。
    *   每次先讨论并确认方案；一次方案落地对应一个任务。
    *   每个任务快照都必须有文档沉淀，避免氛围编程的“无文档执行”。
    *   文档用于记录该次方案与执行过程，不承担持续维护“是否过时”的职责；否则会显著增加维护成本并拖慢快速迭代。
*   **方案两文档工作流（最高优先级，强约束）**：
    1.  **方案讨论 -> 摘要文档（人工全审）**：
        * 先与 AI 持续讨论并反复修订，确认后由 AI 产出“自然语言摘要文档”；该文档必须由人工完整审阅通过。
        * 摘要文档是唯一的“解释层”：所有逻辑细节、术语定义、边界条件、关键取舍、失败策略都必须在此阶段做到理论自洽与逻辑链条闭环。
        * 对于关键算法，摘要文档必须补充相关公式、严格定义，或者简单的伪代码 / 步骤化描述，保证人工在审阅阶段就能完成逻辑闭环判断，而不是只看到接口骨架或结论。
        * 摘要文档里的函数签名、伪代码和流程代码块，默认只用于表达逻辑主干、对象来源、阶段顺序与失败语义，不要求展开最终 Rust 可编译签名。
        * 摘要文档不纠结语法细节、生命周期、借用形式、错误包装噪音或 PyO3 暴露细节；这些属于执行文档与最终源码阶段解决的问题。
        * 但摘要文档不能因为“只写伪代码”而省掉会改变控制流的契约信息；凡是会直接报错、会改变分支选择、会影响对象来源的语义，都必须在自然语言或伪代码层明确写清楚。
        * 好的摘要文档不纠结语法细节，但必须把方案、架构、算法和边界讨论清楚。
        * 自省标准是：即使换个语言重新开发，也不容易跑偏；可以用这句话反过来自省文档是否真的足够清楚。
    2.  **执行文档（AI 审阅 + AI 执行）**：
        * 在摘要通过后，由 AI 直接编写“执行文档”。
        * 执行文档应尽量简化，以可执行为第一目标，不承担完整解释职责，只有在避免歧义所必需时才做最小解释。
        * 执行文档应更侧重流程规划、关键代码、接口约束、文件修改清单与执行步骤，而不是重复摘要文档里的方案解释。
        * 执行文档必须包含关键示例代码，并明确指出需要修改的文件、模块、接口、迁移路径、破坏性更新、删除旧代码与不保留兼容层的处理方式。
        * 执行文档必须前置考虑大文件拆分与模块边界；凡是落地后可能形成巨型文件的实现，必须先在文档中写清拆分方案、文件职责与迁移路径，避免先实现再事后拆分，确保最终代码仍符合代码品味要求。
        * 执行前先由 AI 审阅并产出报告，执行后把更新报告补充回该文档；人工只审阅 AI 报告。
*   **文档表达规范（强约束）**：
    *   文档应优先使用自然语言解释完整思路，尽量减少变量化、符号化表达。
    *   除非为了精确定义算法接口，否则不应使用过多公式、缩写变量或代码式记法。
    *   阶段分工必须明确：逻辑解释只允许放在摘要文档；执行文档只保留落地所需的实现表达、关键代码示例、文件修改清单与执行步骤，不再重复“为什么”。
    *   **摘要正文采用最终态表述**：
        *   主正文只定义当前正式设计，不记录文档演化过程。
        *   禁止在主算法、正式契约、对象定义、边界条件、输入输出说明中使用“改成了……”“不再……”“继续……”“仍然……”这类相对旧文档的表述。
        *   迁移说明、废弃路线、破坏性更新必须单列，不得混入正文主干。
    *   不是所有内容都值得重文档；简单、低耦合、可直接从源码一眼确认的局部改动，不需要强行写成超细方案。
    *   但对**高耦合、不可视、容易静默出错**的链路，必须优先把文档写清楚，再进入实现。典型场景包括：多周期时间轴映射、warmup 传播、容器切片与重基、WF 窗口规划、stitched 拼接、状态传播与资金列重建。
    *   判断标准不是“代码长不长”，而是：一旦写错，是否可能不报错、却悄悄污染结果；这类 quietly wrong 风险链路必须重文档、重边界、重失败策略。
    *   因此，文档不是为了覆盖一切，而是为了优先约束那些最难靠直觉、最难靠肉眼、最难靠零散测试兜住的关键真值链。
*   **任务快照原则（配套约束）**：
    *   文档是任务快照，只记录一次任务决策与执行，不承担长期“全局最新”维护责任。
    *   默认按任务时间理解上下文：一般最新任务就是当前版本参考。
    *   若存在歧义或需最终确认，直接以源代码为准。
    *   文档本身不对“是否过时”负责；不做全局过时治理，也不要求回改历史任务文档。
*   **任务索引（轻量）**：
    *   维护单一索引文件：`doc/tasks/TASK_INDEX.md`。
    *   索引只记录“任务名 + 5~10 句任务摘要”，用于搜索和回找上下文。
    *   索引不做“是否过时”判断，不标记过时任务，不承担版本治理职责。
    *   每次新增任务时，只追加一条索引记录；历史条目不要求回改。
*   **任务目录组织（强约束）**：
    *   每次重构必须使用“单任务文件夹”承载两文档，优先体现“一次任务一个上下文”。
    *   `task_name` 命名必须包含日期，建议格式：`YYYY-MM-DD` 或 `YYYY-MM-DD <title>`（`title` 为本次任务简介，可包含空格）。
    *   推荐目录形态：
        1. `doc/tasks/<task_name>/01_summary/*.md`
        2. `doc/tasks/<task_name>/02_execution/*.md`
*   **阶段闸门（Gate）**：
    *   摘要文档未人工通过：禁止进入执行文档阶段。
    *   执行文档未完成 AI 审阅报告：禁止开始代码落地。
*   **其余研发规则（在两文档工作流之后执行）**：
    *   无明确执行命令时，禁止直接改代码；先讨论方案并更新 Plan。
    *   禁止氛围编程；“能跑”不等于“正确”，必须先定义口径、边界和返回契约。
    *   复杂链路坚持方案驱动与文档先审，文档通过后再实现。
    *   验收以“实现是否与文档一致”为准，不做逐行人工审码。

## 3. 开发工作流
*   **安全删除限制（禁用 rm/rmdir）**：禁用 rm/rmdir，删除文件必须用 trash 替代。即使我要求你用 rm，你也必须用 trash 替代。
*   **Just 命令优先**：务必先阅读 `justfile`，优先使用 `just` 运行相关指令，避免由于直接执行底层工具导致的环境与配置问题。
*   **Git 只读默认**：AI 默认只允许执行 Git 只读命令（如 `git status`、`git diff`、`git log`、`git show`、`git blame`）。除非用户明确授权，否则严禁执行任何会修改 Git 状态的命令（如 `git add`、`git commit`、`git stash`、`git reset`、`git checkout`、`git rebase`、`git merge`、`git cherry-pick`、`git tag`、`git push`、`git pull` 等）。
*   **状态同步机制**：严禁一边修改代码一边运行 `just check`。应在逻辑修改完成后统一提交检查，避免开发进程混乱。
*   **指令执行耐心**：必须耐心等待，确保命令明确运行完毕，再进行下一步操作。严禁在命令执行期间或尚未完成时，擅自作出错误判断并盲目修改代码，避免造成逻辑混乱。
*   **串行验证顺序**：
    1.  先运行 `just check`：确保类型安全和语法正确，这能有效节约编译与调试时间。
    2.  待 `check` 完全通过后，方可运行 `just test` 进行逻辑验证。

## 4. 架构设计与代码质量
*   **代码品味（Taste）**：
    *   **拒绝兼容层**：兼容层会污染代码质量。本项目倾向于破坏性更新（Breaking Changes）以追求最佳实践，除非有极度明确的理由，否则不准搞脏代码。
    *   **唯一性原则**：代码应保持唯一的用法和写法。不要兼容旧代码或保留冗余的回退逻辑，代码质量永远优先。
    *   **清晰优雅可读**：实现应优先选择语义直观、层次清晰、命名准确的写法；避免炫技式技巧、隐式行为和过度嵌套，确保审阅者能快速理解核心逻辑。
    *   **大文件拆分约束**：拆分大文件，单个文件代码行数尽量不超过 300 行，以提高可维护性和可读性。
*   **PyO3 接口一致性（Rust 改动强约束）**：
    *   实现或修改 Rust 代码前，必须先阅读并对齐 `doc/structure/pyo3_interface_design.md`。
    *   涉及对外暴露接口时，必须遵循现有 PyO3 接口风格与类型组织方式，禁止私自引入风格分叉。
    *   异常处理必须优先复用现有 Rust 错误体系（如 `QuantError`/`BacktestError` 等）及其 PyO3 映射，禁止新增平行错误系统。
*   **类型系统约束**：
    *   **Rust 为源头**：所有核心类型在 Rust 端定义，利用 `pyo3-stub-gen` 自动生成 Python 的 `.pyi` 类型存根。
    *   **禁止镜像维护**：Python 侧不得再维护与 Rust 对应的镜像输入/输出类型文件；类型变更必须先改 Rust，再通过 `just stub` / `just check` 同步存根与静态检查。
    *   **Python 端强类型**：对于大型项目，Python 侧应尽可能结合 **Pydantic** 进行类型声明，确保项目的健壮性与稳定性。

## 5. 技术指标对齐标准
*   **基准对齐**：回测引擎中的指标实现必须严格以 **`pandas-ta` (talib=true)** 模式为准。所有指标实现必须通过与之对齐的严格测试。
*   **库优先级**：`pandas-ta` (talib=true) > `pandas-ta` (talib=false)。禁止直接使用原始的 `talib` 库。
*   **调用规范**：禁止使用类似 `ohlc.ta.atr(talib=True)` 的调用方式。应统一采用 `import pandas_ta as ta; ta.atr(...)`。
*   **版本背景与警告**：
    *   **优先使用旧版 `pandas-ta`**：原版 `pandas-ta` 已不再更新且已被作者删库闭源。本项目目前使用的是该库的一个不再更新的**备份旧版**，它是我们指标对齐的基准，必须优先使用。
    *   **慎用 `pandas-ta-classic`**：这是社区基于原版 Fork 的新版，但其内部包含大量未经验证的效果修改。除非备份旧版中确实缺失某个指标，否则不要使用新版（Classic），以防止测试失败或回测结果发生不可解释的漂移。

## 6. 异常处理与防御原则
*   **拒绝异常吞噬**：严禁滥用 `try: except Exception: pass`。异常捕捉必须精细化，或至少打印详细的项目报错日志。
*   **拒绝冗长防御**：不搞过度复杂的防御性编程。本项目推崇直接报错（Exception），通过明确的报错约束来引导用户使用唯一的正确写法。
*   **明确性胜过容错**：在回测引擎中，不搞折中的回退逻辑或警告，直接报错是保证逻辑唯一性与系统稳健性的最佳方式。
*   **Rust 库层硬约束**：在 Rust 代码中，只要能明确判定为错误，就直接返回明确异常；不要做兼容回退或静默兜底。Rust 作为库层应优先保证行为确定性与错误可诊断性。

## 7. Notebook 与策略脚本分层设计
*   **职责分离**：`.py` 是给 AI 调试的，`ipynb` 是给人调试的。
*   **研究策略入口**：`strategy_hub` 研究策略统一使用 `just workflow ...`，不要直接执行策略模块文件。
*   **AI 读取约束**：AI 默认不得直接读取 `ipynb`；只有在用户给出明确指令时，才允许读取 `ipynb` 内容。
*   **AI 执行优先级**：优先使用 `just workflow --strategies <strategy> --symbols <symbol> --mode <modes>`；不要虚构不存在的命令。
*   **Notebook 约束**：`ipynb` 应复用 `strategy_hub` 统一入口，不再要求每个研究策略模块单独提供 notebook 专用包装函数。

## 8. 策略调试评估口径（强约束，仅限 research）
*   **适用范围**：本节仅适用于 `py_entry/strategy_hub/search_spaces` 下的 research 策略。
*   **执行入口**：统一使用 `just workflow --strategies <strategy> --symbols <symbol> --mode ...`。
*   **结果口径**：AI 应优先摘要 `just workflow` 生成的 JSON 日志，不要只贴命令行输出。
*   **摘要最小内容**：每条 `walk_forward` 结果至少列出 `strategy_name`、`symbol`、`performance.calmar_ratio_raw`、`performance.total_return`、`performance.max_drawdown`。
*   **决策规则**：默认按 `performance.calmar_ratio_raw > performance.total_return > -performance.max_drawdown` 比较；`performance.calmar_ratio` 仅作辅助参考。
